---
phase: 01-supabase-auth-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: []
autonomous: false
requirements: [DATA-02, DATA-03, DATA-05]
user_setup:
  - service: supabase
    why: "Apply the Phase 1 schema — this environment has NO database access (no Supabase MCP, no `supabase db push`, no connection string), so the SQL must be run by hand"
    dashboard_config:
      - task: "Run supabase/profiles.sql, then supabase/photos-storage.sql, in the SQL editor"
        location: "Supabase Dashboard → SQL Editor → New query → paste each file's full contents → Run"

must_haves:
  truths:
    - "Running supabase/profiles.sql creates public.profiles with RLS enabled, every Phase-3 column, and the four DATA-05 payment columns"
    - "Running supabase/photos-storage.sql creates a photos bucket that is NOT reachable via a public URL"
    - "A two-account test confirms a user can read/write only their own profiles row (never another user's row)"
    - "A two-account test confirms a user can read/write only their own {userId}/ photo path (never another user's path)"
    - "The four DATA-05 payment/eligibility columns are present on the applied profiles table"
  artifacts:
    - path: "supabase/profiles.sql"
      provides: "The schema file the human applies (must exist before this plan runs)"
      contains: "is_founding_member boolean"
    - path: "supabase/photos-storage.sql"
      provides: "The private-bucket file the human applies (must exist before this plan runs)"
      contains: "'photos', 'photos', false"
  key_links:
    - from: "applied public.profiles RLS"
      to: "auth.users identity"
      via: "two-account test proves (select auth.uid()) = id blocks cross-user reads/writes"
      pattern: "auth.uid"
    - from: "applied photos bucket"
      to: "public internet"
      via: "a public URL to an object returns not-authorized / not-found (bucket is private)"
      pattern: "photos"
---

<objective>
Prove the secured per-user data path actually works against the live Supabase project: apply the two SQL files from plan 01-02, then verify owner-only RLS (two-account test), a private (publicly-unreachable) photo bucket, and the presence of the DATA-05 payment columns.

Purpose: this is the phase's end-to-end verification. Because this environment has NO database access, the SQL from 01-02 and the client/middleware from 01-01 pass build/type checks WITHOUT the schema ever being applied — a false-positive verification risk. This plan closes that gap with a human-run apply step and a hands-on two-account proof, satisfying DATA-02's explicit "verified with a two-account test", DATA-03's "confirmed unreachable via public URL", and confirming DATA-05's columns landed.

Output: a completed 01-03-SUMMARY.md recording the apply result and the two-account / private-bucket / payment-column verification outcomes. No code files are produced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-supabase-auth-foundation/01-RESEARCH.md
@supabase/profiles.sql
@supabase/photos-storage.sql
</context>

## Phase Goal

Pure-infrastructure phase (no UI). The MVP "vertical slice" is the **complete secured per-user data path**. Plan 01-01 built the client/middleware spine and 01-02 wrote the schema; this plan is the closest analog to a failing-then-passing end-to-end test for a no-live-DB environment: the two-account RLS proof plus the private-bucket check. Framed as an infrastructure outcome:

> **As** the Campus Crush team, **I want to** apply the Phase 1 schema and confirm with a two-account test that a user can touch only their own row and only their own photo folder, **so that** Phase 2 sign-in and Phase 3 onboarding can persist private data with a proven, not merely assumed, RLS boundary.

## Artifacts this phase produces

| Artifact | Symbol / shape | Plan | This plan? |
|----------|----------------|------|------------|
| Applied `public.profiles` table + RLS in the live project | verified via two-account SQL test | 01-03 | ✅ verifies |
| Applied private `photos` bucket + storage RLS in the live project | verified via public-URL-unreachable check | 01-03 | ✅ verifies |
| `supabase/profiles.sql`, `supabase/photos-storage.sql` | the files being applied | 01-02 | — |
| `lib/supabase/{client,server,middleware}.ts`, `middleware.ts` | SSR client factories + session middleware | 01-01 | — |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Apply supabase/profiles.sql and supabase/photos-storage.sql in the Supabase SQL editor</name>
  <read_first>
    - supabase/profiles.sql (the exact SQL to paste and run — the profiles table + owner-only RLS + DATA-05 payment columns)
    - supabase/photos-storage.sql (the exact SQL to paste and run — the private bucket + storage RLS)
    - .planning/phases/01-supabase-auth-foundation/01-RESEARCH.md (## Open Questions → confirm no pre-existing `profiles` table/view collides; both files use `if not exists` / `on conflict do nothing`, so a re-run is safe)
  </read_first>
  <action>Human-only apply step (no CLI/MCP/connection string exists in this environment): paste and run `supabase/profiles.sql` then `supabase/photos-storage.sql` in the Supabase dashboard SQL editor, then confirm the `profiles` table and a private `photos` bucket appear. Report any error verbatim. No repository files are changed by this task.</action>
  <what-built>The two schema files from plan 01-02 are ready to apply. This environment cannot run them (no Supabase MCP, no `supabase db push`, no connection string), so they must be executed by hand in the Supabase dashboard — exactly the project's established manual-execution pattern for `founding-member.sql` / `venues.sql`.</what-built>
  <how-to-verify>
    1. Open the campus-crush Supabase project → SQL Editor → New query.
    2. Paste the FULL contents of `supabase/profiles.sql` and click Run. Expect "Success. No rows returned" (idempotent — re-running is safe via `if not exists`). If it errors because a `profiles` table/view already exists, stop and report the exact message before proceeding.
    3. New query → paste the FULL contents of `supabase/photos-storage.sql` → Run. Expect success; the `photos` bucket appears under Storage.
    4. In Storage, confirm the `photos` bucket is listed as PRIVATE (not public).
  </how-to-verify>
  <acceptance_criteria>
    - Both `supabase/profiles.sql` and `supabase/photos-storage.sql` ran without error in the SQL editor (or any error is reported verbatim in the resume signal).
    - A `public.profiles` table is visible in the Table editor and a `photos` bucket is visible under Storage, marked private.
  </acceptance_criteria>
  <resume-signal>Type "applied" once both files ran successfully, or paste the exact error message if either failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-account RLS proof + private-bucket check + DATA-05 column confirmation</name>
  <read_first>
    - .planning/phases/01-supabase-auth-foundation/01-RESEARCH.md (## Code Examples → "Two-account verification procedure" steps 1-4, approx. lines 479-483 — the SQL-editor role/JWT-claim impersonation recipe)
    - supabase/profiles.sql (the column list to confirm the four DATA-05 columns landed)
  </read_first>
  <action>Human-only verification against the live project: run the two-account impersonation recipe in the SQL editor to prove a user can touch only their own `profiles` row (cross-user reads/writes blocked both directions), open a `photos` object's public URL while signed out to prove the bucket is private, and confirm the four DATA-05 columns exist. Report which check fails, if any. No repository files are changed by this task.</action>
  <what-built>The applied schema enforces owner-only access at the database layer (RLS on `profiles` and `storage.objects`) and a private photo bucket. This checkpoint proves that boundary holds with two distinct user identities, satisfying DATA-02's "verified with a two-account test" and DATA-03's "confirmed unreachable via public URL", and confirms the DATA-05 columns exist.</what-built>
  <how-to-verify>
    1. In the SQL editor, impersonate User A: `set local role authenticated; set local "request.jwt.claims" = '{"sub":"<user-a-uuid>"}';` then `insert` and `select` a `profiles` row where `id = '<user-a-uuid>'` — both succeed.
    2. Still as User A, `select`/`update` a row where `id = '<user-b-uuid>'` — expect ZERO rows / rejection (cross-user access blocked).
    3. Repeat steps 1-2 with User B against User A's row — same result (blocked).
    4. Confirm the private bucket: in the Storage UI, get (or construct) the public URL for any object under `photos/` and open it in a signed-out browser tab — expect a "not authorized" / not-found response, NOT the image (DATA-03). A signed URL via `createSignedUrl` for the owner's own path is the only path that should return the object.
    5. Confirm DATA-05 columns: in the Table editor (or `select column_name from information_schema.columns where table_name = 'profiles'`), verify `is_founding_member`, `payment_required`, `stripe_customer_id`, and `stripe_payment_method_id` all exist.
  </how-to-verify>
  <acceptance_criteria>
    - Owner reads/writes of their own `profiles` row succeed; cross-user reads/updates return zero rows or are rejected (both directions A→B and B→A).
    - A public URL to a `photos` object does NOT return the image when signed out (bucket is private); only an owner-scoped signed URL returns it.
    - All four columns `is_founding_member`, `payment_required`, `stripe_customer_id`, `stripe_payment_method_id` are present on `public.profiles`.
  </acceptance_criteria>
  <resume-signal>Type "verified" if all three checks pass (owner-only RLS both directions, private bucket, DATA-05 columns present), or describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → Postgres | The two-account test exercises this boundary directly to prove RLS holds |
| signed-out internet → Storage object | The public-URL check exercises this to prove the bucket is not public |
| human operator → live Supabase project | The apply step runs privileged SQL by hand; the operator is trusted, but a false-positive (schema never applied) is the real risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FALSEPOS | Repudiation / verification gap | "Build passes but schema never applied" | mitigate | Blocking human-action apply step (Task 1) plus a hands-on two-account + public-URL proof (Task 2) — the only way to close the no-DB-access false-positive gap identified by the schema gate |
| T-01-IDOR-PROOF | Information Disclosure | `profiles` / `storage.objects` cross-user access | mitigate | Two-account test asserts cross-user reads/writes are blocked in BOTH directions before the phase is marked verified (ASVS V4) |
| T-01-BUCKET-PROOF | Information Disclosure | Face-photo bucket public exposure | mitigate | Public-URL-unreachable check confirms `public = false` actually took effect in the live project, not just in the SQL file |
</threat_model>

<verification>
- Task 1: both SQL files applied without error; `public.profiles` and a private `photos` bucket exist in the live project.
- Task 2: owner-only RLS proven with two accounts (cross-user access blocked both directions); public URL to a photo object is unreachable when signed out; all four DATA-05 columns confirmed present.
- Any failure at either checkpoint blocks phase completion and is reported verbatim for follow-up (potential 01-02 SQL fix + re-apply).
</verification>

<success_criteria>
The Phase 1 schema is applied to the live Supabase project and proven correct: a two-account test confirms owner-only access to `profiles`, the `photos` bucket is confirmed private (unreachable via public URL), and the four DATA-05 payment/eligibility columns are present (ROADMAP Phase 1 success criteria #1, #2, #5 — the "verified" half of the phase goal).
</success_criteria>

<output>
Create `.planning/phases/01-supabase-auth-foundation/01-03-SUMMARY.md` when done.
</output>
